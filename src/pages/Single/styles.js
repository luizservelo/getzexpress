import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    postContainer: {
        color: '#191919',
        padding: 15
    },
    title: {
        textAlign: 'center',
        fontSize: 18,
        fontWeight: 'bold'
    }

});

export default styles;